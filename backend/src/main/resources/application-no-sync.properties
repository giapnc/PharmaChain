# Application Configuration - NO AUTO SYNC
# Use this profile to disable blockchain indexing for testing
# Usage: mvn spring-boot:run -Dspring-boot.run.profiles=no-sync

# Disable scheduling (no auto-sync from blockchain)
spring.task.scheduling.enabled=false

# All other configs inherit from application.properties

