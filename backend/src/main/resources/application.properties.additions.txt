# QR Code Configuration
# Thêm các config này vào file application.properties hiện tại

# QR Code settings
qr.code.base-url=http://localhost:8080/api/public/verify
qr.code.size=300
qr.code.storage-path=./qr-codes

# File upload settings (nếu dùng local storage)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Optional: S3/MinIO configuration (nếu dùng cloud storage)
# aws.s3.bucket-name=your-bucket-name
# aws.s3.region=us-east-1
# aws.s3.access-key=your-access-key
# aws.s3.secret-key=your-secret-key

